@using Log4JDash.Web.Mvc
@model DateTime
@{
    Layout = "_Editor.cshtml";

    var attributes = new Dictionary<string, object> ();
    var modelMetadata = ViewData.ModelMetadata;
    DefaultValueUtil.PopulateHtmlAttributes (modelMetadata, attributes);
    var format = modelMetadata.EditFormatString;
}
@Html.TextBox("", Model, format, attributes)
